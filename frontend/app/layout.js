
export const metadata = { title: 'x402frog8s', description: 'x402-based frontend' };
export default function RootLayout({ children }) {
  return (<html lang="en"><body style={{ margin:0, background:'#000', color:'#fff', fontFamily:'sans-serif' }}>{children}</body></html>);
}
